# Flask User API

A simple REST API built with Flask that manages user data.  
Supports basic CRUD operations (Create, Read, Update, Delete).

## 🚀 Features
- Add a new user (POST /users)
- Get all users (GET /users)
- Update a user by ID (PUT /users/<id>)
- Delete a user by ID (DELETE /users/<id>)

## 🛠 Tools Used
- Python
- Flask
- curl (for testing)

## ▶️ How to Run
1. Clone this repo:
   ```bash
   git clone https://github.com/Vaishnavi-1151/FlaskUserAPI.git
   cd FlaskUserAPI
